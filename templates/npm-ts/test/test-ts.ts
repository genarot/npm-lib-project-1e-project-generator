import  {expect} from 'chai';
import lib from './../lib/index';


describe('<%= projectName %> tests', () => {
    
    it('Suma correcta', () => {
        
        expect(1 + 1).to.equal(2);
    });
});
